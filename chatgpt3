private async Task ConfirmDelete(int id)
    {
        bool isConfirmed = await js.InvokeAsync<bool>("confirm", "Are you sure you want to delete this employee?");
        if (isConfirmed)
        {
            await service.DeleteEmployee(id);
            empList = await service.GetAll(); // Refresh list after deletion
        }
    }
